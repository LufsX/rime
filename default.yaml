# Rime default settings
# encoding: utf-8

config_version: "2023-04-09"

schema_list:
  - schema: lufs_flypy # 小鹤双拼
  - schema: lufs_pinyin # 朙月拼音

switcher:
  caption: "[方案菜单]"
  hotkeys:
    - F2
  save_options:
    - ascii_mode
    - full_shape
    - ascii_punct
    - traditionalization
    - emoji_suggestion
  fold_options: true # 折叠选项
  abbreviate_options: true # 简化选项
  option_list_separator: "/"

menu:
  page_size: 4

punctuator:
  full_shape:
    __include: punctuation:/full_shape
  half_shape:
    __include: punctuation:/half_shape

ascii_composer:
  good_old_caps_lock: false
  switch_key:
    Shift_L: noop
    Shift_R: commit_code # macOS 上此项无效，Windows 可用右 Shift 切换中英输入
    Control_L: noop
    Control_R: noop # macOS 上此项无效，Windows 上可自行定义
    Caps_Lock: commit_code
    Eisu_toggle: clear

key_binder:
  select_first_character: "bracketleft" # lua 选词快捷键，选第一个字
  select_last_character: "bracketright" # lua 选词快捷键，选最后一个字

  bindings:
    # 翻页
    - { when: paging, accept: Tab, send: Page_Down } # Tab 向下翻页
    - { when: paging, accept: Shift+Tab, send: Page_Up } # Shift + Tab 向上翻页

    - { when: paging, accept: equal, send: Page_Down } # = 向下翻页
    - { when: paging, accept: minus, send: Page_Up } # - 向上翻页

    # 需要注释掉部分冲突的正则匹配才可用，详情见: https://github.com/LufsX/rime/issues/15#issuecomment-1496011933
    # - { when: paging, accept: period, send: Page_Down } # 句号向下翻页
    # - { when: paging, accept: comma, send: Page_Up } # 逗号向上翻页

    # 失效设置，可通过关闭/更改 lua 选词的快捷键恢复
    # - { when: paging, accept: bracketright, send: Page_Down } # 右括号向下翻页
    # - { when: paging, accept: bracketleft, send: Page_Up } # 左括号向上翻页

    # 候选操作
    - { when: has_menu, accept: semicolon, send: 2 } # 输入时「;」二选
    - { when: has_menu, accept: Control+1, send: 3 } # 输入时「'」三选哈昂

    # Rime key bindings
    # emacs_editing
    # - { when: composing, accept: Control+p, send: Up }
    # - { when: composing, accept: Control+n, send: Down }
    # - { when: composing, accept: Control+b, send: Left }
    # - { when: composing, accept: Control+f, send: Right }
    # - { when: composing, accept: Control+a, send: Home }
    # - { when: composing, accept: Control+e, send: End }
    # - { when: composing, accept: Control+d, send: Delete }
    # - { when: composing, accept: Control+k, send: Shift+Delete }
    # - { when: composing, accept: Control+h, send: BackSpace }
    # - { when: composing, accept: Control+g, send: Escape }
    # - { when: composing, accept: Control+bracketleft, send: Escape }
    # - { when: composing, accept: Control+y, send: Page_Up }
    # - { when: composing, accept: Alt+v, send: Page_Up }
    # - { when: composing, accept: Control+v, send: Page_Down }
